export type GenericType = any;

export interface UserForm {
  first_name: string;
  last_name: string;
  email: string;
  nickname: string;
  address: string;
  phoneNumber: string;
}

export interface UserPages {
  buttonName?: string;
}
